package server

import "testing"

func TestStageTiles(t *testing.T) {
	// b := NewBoard()
	// tiles := []Tile{WhiteTile, WhiteTile, WhiteTile}
	// err := b.StageTiles(tiles, 2)
	// err = b.StageTiles([]Tile{BlackTile}, 2)

	// if err != nil {

	// 	t.Fatalf("%v %s", b.Staged, err.Error())
	// }

}
